/**
 * ⚠️ DEPRECATED — Questo script non funziona più dopo la riorganizzazione di scripts/ in subdir.
 * I pattern di replacement in SCRIPT_REPLACEMENTS matchano `../src/lib/` (vecchio path)
 * ma ora tutti gli script sono in subdir a 2 livelli di profondità (../../src/lib/).
 *
 *Archiviato per riferimento storico. Non eseguire.
 *
 * Se serve uno script simile in futuro, ricreare con pattern per `../../src/lib/`.
 */